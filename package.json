// File: your-vercel-project/package.json
{
  "name": "your-vercel-project-name",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    // **** REMOVE chrome-aws-lambda ****
    // "chrome-aws-lambda": "^10.1.0",
    "node-fetch": "^2.6.7", // Keep if needed
    // **** ADD @sparticuz/chromium ****
    "@sparticuz/chromium": "^119.0.0", // Or latest stable version
    // **** Use a RECENT puppeteer-core ****
    "puppeteer-core": "^19.11.1" // Or align with @sparticuz/chromium if needed, check its docs
  },
  "engines": {
    "node": "18.x"
  }
}
